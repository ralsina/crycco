# Prose Fixture

A pure markdown file, with a nested list:

* top level item
  * nested item

An indented code block:

    indented code

And a fenced, syntax-highlighted block:

```crystal
puts "hello"
```
